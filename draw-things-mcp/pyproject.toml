[project]
name = "draw-things-mcp"
version = "0.1.0"
description = "MCP server for Draw Things — local AI image generation"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
draw-things-mcp = "draw_things_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
